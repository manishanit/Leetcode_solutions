class Solution {
public:
    string defangIPaddr(string address) {
        string address1="";
        for(int i=0;i<address.length();i++)
        {
            if(address[i]=='.')
                address1+="[.]";
            else
                address1+=address[i];
        }
        return address1;
    }
};
